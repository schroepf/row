package de.mistatee.erglog.data.concept2.logbook.profile.model

/** The Logbook Account holder's profile data, as returned by Concept2's `/api/users/{user}` endpoint. */
data class Profile(val username: String)
